# Login-and-Registration-System-Project-Cpp
This is developed using C++ login and registration system.Create a registration function that takes username and password as input.  Validate the inputs and check for duplicate usernames if needed.  Store user credentials securely in a file.Login function that reads credentials and verifies.Give success or error messages for registration and login.
